# ----------------------------------------------------------------
# QuantumAlpha Environment Variables Example
# ----------------------------------------------------------------

# This file contains all environment variables used by the QuantumAlpha project.
# Copy this file to .env and fill in your desired values.

# ----------------------------------------------------------------
# 1. Docker Compose Overrides (Used by docker-compose.yml)
# ----------------------------------------------------------------

# PostgreSQL/TimescaleDB Configuration
DB_USER=quantumalpha_user
DB_PASSWORD=quantumalpha_pass
DB_NAME=quantumalpha_db
DB_PORT=5432

# Redis Configuration
REDIS_PORT=6379

# Kafka Configuration
KAFKA_PORT=9092

# Grafana Admin Credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ----------------------------------------------------------------
# 2. Backend Configuration (Used by backend/common/config.py)
#    Note: Some variables are redundant with Docker overrides but are included
#    for clarity and for non-Docker local development.
# ----------------------------------------------------------------

# PostgreSQL/TimescaleDB Connection (Used if not running in Docker)
# DB_HOST=localhost
# DB_USERNAME=postgres
# DB_PASSWORD=postgres
# DB_NAME=quantumalpha

# Redis Connection (Used if not running in Docker)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# InfluxDB Configuration (for time-series data)
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=your_influxdb_token
INFLUXDB_ORG=quantumalpha
INFLUXDB_BUCKET=market_data

# MongoDB Configuration (for document storage)
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_USERNAME=
MONGODB_PASSWORD=
MONGODB_DATABASE=quantumalpha

# API Keys for External Data Sources
ALPHA_VANTAGE_API_KEY=
POLYGON_API_KEY=
NEWS_API_KEY=

# Broker Configuration (Alpaca Example)
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_ENDPOINT=https://paper-api.alpaca.markets

# ML Configuration
MODEL_REGISTRY_PATH=/path/to/model/registry

# Service Host/Port (Used if not running in Docker)
# DATA_SERVICE_HOST=localhost
# DATA_SERVICE_PORT=8081
# AI_ENGINE_HOST=localhost
# AI_ENGINE_PORT=8082
# RISK_SERVICE_HOST=localhost
# RISK_SERVICE_PORT=8083
# EXECUTION_SERVICE_HOST=localhost
# EXECUTION_SERVICE_PORT=8084

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPIC_MARKET_DATA=market_data
KAFKA_TOPIC_SIGNALS=signals
KAFKA_TOPIC_ORDERS=orders

# ----------------------------------------------------------------
# 3. Frontend Configuration (Used by web-frontend)
# ----------------------------------------------------------------

# Base URL for the API Gateway.
# For local development (backend running on port 8080):
VITE_API_BASE_URL=http://localhost:8080
